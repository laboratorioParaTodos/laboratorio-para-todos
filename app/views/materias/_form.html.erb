<%= bootstrap_form_for(@materia) do |f| %>
    <%= f.text_field :clave %>
    <%= f.text_field :nombre %>
    <%= f.select :carrera_id, options_for_select(@carreras.map {|c| [c.nombre, c.id]}) %>
    <%= f.submit 'Guardar', class: 'btn btn-default center-block' %>
<% end %>
